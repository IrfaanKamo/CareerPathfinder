@{
    Layout = "~/Areas/Admin/Views/Shared/_TestLayout.cshtml";
    ViewBag.Title = "10";
}

@model ProjectPathfinder.Areas.Member.ViewModels.TestStepTen

<div class="section">
    <div class="container">
        <div class="panel panel-success">
            <div class="panel-body">
                
                @using (Html.BeginForm())
                {
                    @Html.Partial("~/Views/AreaPartials/_Step_10.cshtml", Model)

                    <ul class="list-unstyled">
                        <li class="pull-right">
                            <a href="@Url.Action("Step_11_1", "MemberTest", new {id = ViewBag.CurrentMemberId })" class="btn btn-primary test-button">
                                Next Step <i class="glyphicon glyphicon-chevron-right"></i>
                            </a>
                        </li>
                        <li class="pull-left">
                            <a href="@Url.Action("Step_09", "MemberTest", new {id = ViewBag.CurrentMemberId })" class="btn btn-primary test-button cancel">
                                <i class="glyphicon glyphicon-chevron-left"></i> Previous Step
                            </a>
                        </li>
                    </ul>
                }
                
            </div>
        </div>
    </div>
</div>

